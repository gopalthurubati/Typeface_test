>we request the web by using urllib method
>we retrive URl from web using BeautifulSoup vs4 method
from urllib.request import urlopen
from bs4 import BeautifulSoup

url = "some website url"
response = urlopen(url)
img_all= 
for all_images in soup_1.find(attrs={'class': 'some class name'}).find_all('img', scr=True)
                text = img.text
                img = img[src]
>Buy using BeautifulSoup we retrive the imagestag that having src
>we retrive SS method and test the web 